import { ResetPasswordForm } from "@/components/forms/reset-password-form";

export const metadata = {
  title: "Reset Password | AtomQuest Portal",
};

export default function ResetPasswordPage() {
  return <ResetPasswordForm />;
}
