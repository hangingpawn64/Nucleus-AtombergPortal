# Styles

Global Tailwind and design token setup lives in `src/app/globals.css`.

Use this folder for shared CSS modules or future theme-specific files only when a component-level Tailwind class is not the clearest option.
